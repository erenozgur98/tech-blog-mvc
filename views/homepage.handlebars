{{#each posts as |post| }}
<div class="row mb-4 post">
    <div class="col-md-5">
        <h2>
            <a href="/post/{{id}}">{{post.post_title}}</a>
        </h2>
        <p>{{post.post_content}}</p>
    </div>
</div>
{{/each}}